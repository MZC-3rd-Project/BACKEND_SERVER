# Module: `libs/config/kafka-core`

## 한눈에 보기
- 역할: Kafka 공통 속성/기본 의존성 베이스를 제공합니다.
- 사용 시점: Kafka 설정 키(`app.kafka.consumer.*`)를 공통으로 사용할 때 사용합니다.

## 핵심 제공
- `KafkaConsumerProperties`

## 간단 예시
```text
implementation(project(":libs:config:kafka-core"))
```
