package com.example.auth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // Gateway HMAC 검증은 security-starter 모듈에서 자동 구성
    // 추가 보안 설정 필요시 여기에 추가
}
